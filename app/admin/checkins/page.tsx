import { CheckCircle2, Clock3, QrCode, UsersRound } from "lucide-react";
import { AdminShell } from "@/components/admin-shell";
import { CheckinManager } from "./checkin-manager";

export default function CheckinsPage(){const metrics=[[UsersRound,"승인 참가자","142"],[CheckCircle2,"체크인 완료","98"],[QrCode,"QR 체크인","91"],[Clock3,"최근 1시간","64"]] as const;return <AdminShell active="체크인 관리"><header className="admin-top"><div><span className="eyebrow">On-site operations</span><h1 className="page-title" style={{fontSize:34}}>체크인 관리</h1><p className="page-copy">Fashion Networking Day · 2027.03.20 · 성수 XYZ Seoul</p></div><span className="chip violet">현장 운영 중</span></header><section className="metric-grid">{metrics.map(([Icon,label,value])=><div className="card metric" key={label}><span className="metric-icon"><Icon size={18}/></span><div className="metric-value">{value}</div><div className="metric-label">{label}</div></div>)}</section><section className="section"><div className="section-head"><div><h2 className="section-title">참가자 현장 처리</h2><p className="list-sub">중복 체크인은 자동으로 감지됩니다.</p></div><span className="chip green">체크인율 69%</span></div><CheckinManager/></section></AdminShell>}
