"use client";

import { useState } from "react";
import { Check, Mail, Plus, ShieldCheck, Trash2 } from "lucide-react";

const initialMembers=[{name:"김한재",email:"hanjae@noogu.kr",role:"Owner"},{name:"박지수",email:"jisu@noogu.kr",role:"Admin"},{name:"이서준",email:"staff@noogu.kr",role:"Staff"}];

export function SettingsClient(){const [members,setMembers]=useState(initialMembers);const [saved,setSaved]=useState(false);const save=()=>{setSaved(true);setTimeout(()=>setSaved(false),1800)};return <div className="report-grid section"><div><section className="card card-pad form-grid"><div><h2 className="section-title">조직 프로필</h2><p className="list-sub">행사 랜딩페이지와 발신 메일에 사용됩니다.</p></div><label className="field"><span>조직명</span><input className="input" defaultValue="NOOGU Events"/></label><label className="field"><span>조직 URL</span><input className="input" defaultValue="noogu-events"/></label><div className="two-col"><label className="field"><span>대표 이메일</span><input className="input" defaultValue="event@noogu.kr"/></label><label className="field"><span>브랜드 컬러</span><input className="input" type="color" defaultValue="#bbf34a"/></label></div><label className="field"><span>기본 행사 문의 안내</span><textarea className="textarea" defaultValue="행사 관련 문의는 event@noogu.kr로 연락해주세요."/></label><button className="button green" onClick={save}>{saved?<><Check size={16}/> 저장됨</>:"변경사항 저장"}</button></section><section className="card card-pad section form-grid"><div><h2 className="section-title">운영 기본값</h2><p className="list-sub">새 행사 생성 시 자동 적용됩니다.</p></div><label className="field"><span>기본 공개 방식</span><select className="select" defaultValue="approval"><option value="public">공개 · 즉시 승인</option><option value="approval">공개 · 관리자 승인</option><option value="private">비공개</option></select></label><label className="field"><span>연결 방식</span><select className="select"><option>QR 스캔 즉시 상호 연결</option><option>상대방 수락 후 연결</option></select></label><label className="list-row" style={{cursor:"pointer"}}><input type="checkbox" defaultChecked/><div className="list-content"><p className="list-title">승인 시 이메일 자동 발송</p><p className="list-sub">행사 정보와 체크인 QR 포함</p></div></label></section></div><aside><section className="card card-pad"><div className="section-head"><div><h2 className="section-title">팀 멤버</h2><p className="list-sub">역할에 따라 접근 범위가 달라집니다.</p></div><button className="chip green" style={{border:0}}><Plus size={13}/> 초대</button></div><div className="list">{members.map(member=><div className="list-row" key={member.email}><span className="avatar sm green">{member.name.slice(-2)}</span><div className="list-content"><p className="list-title">{member.name}</p><p className="list-sub">{member.email}</p></div><select className="select" style={{width:92,minHeight:36,padding:"0 8px"}} defaultValue={member.role}><option>Owner</option><option>Admin</option><option>Staff</option><option>Viewer</option></select>{member.role!=="Owner"&&<button className="icon-button" style={{width:34,height:34}} onClick={()=>setMembers(items=>items.filter(item=>item.email!==member.email))}><Trash2 size={14}/></button>}</div>)}</div></section><section className="card card-pad section"><span className="detail-icon"><ShieldCheck size={18}/></span><h2 className="section-title" style={{marginTop:16}}>권한 안내</h2><p className="page-copy">Owner와 Admin은 전체 설정을 변경할 수 있습니다. Staff는 참가 승인, 체크인과 네임택 출력만 가능합니다.</p></section><section className="card card-pad section"><span className="detail-icon"><Mail size={18}/></span><h2 className="section-title" style={{marginTop:16}}>이메일 발신 설정</h2><p className="page-copy">NOOGU 기본 발신 도메인을 사용 중입니다.</p><span className="chip green" style={{marginTop:12}}>도메인 인증 완료</span></section></aside></div>}
